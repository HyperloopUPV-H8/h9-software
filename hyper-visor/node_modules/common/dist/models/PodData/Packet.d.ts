import { PacketUpdate } from "../../adapters";
import { Measurement } from "./Measurement";
export type Packet = {
    id: number;
    name: string;
    hexValue: string;
    count: number;
    cycleTime: number;
    measurements: Measurement[];
};
export declare function updatePacket(boardName: string, packet: Packet, update: PacketUpdate): void;
//# sourceMappingURL=Packet.d.ts.map