import { PacketUpdate } from "../../adapters";
import { Board } from "./Board";
import { Packet } from "./Packet";
export type PodData = {
    boards: Board[];
    packetToBoard: Record<number, number>;
    lastUpdates: Record<string, PacketUpdate>;
};
export declare function updatePodData(podData: PodData, packetUpdates: {
    [id: number]: PacketUpdate;
}): void;
export declare function getPacket(podData: PodData, id: number): Packet | undefined;
//# sourceMappingURL=PodData.d.ts.map