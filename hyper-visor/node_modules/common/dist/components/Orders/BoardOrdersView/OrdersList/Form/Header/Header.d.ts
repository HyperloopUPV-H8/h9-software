import { SpringValue } from "@react-spring/web";
export type HeaderInfo = ToggableHeader | FixedHeader;
type ToggableHeader = {
    type: "toggable";
    isOpen: boolean;
    toggleDropdown: () => void;
};
type FixedHeader = {
    type: "fixed";
};
type Props = {
    name: string;
    disabled: boolean;
    info: HeaderInfo;
    springs: Record<string, SpringValue>;
    onTargetToggle: (state: boolean) => void;
    onButtonClick: () => void;
};
export declare const Header: ({ name, disabled, info, springs, onTargetToggle: onTargetClick, onButtonClick, }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
//# sourceMappingURL=Header.d.ts.map